appId: com.lumosnap.desktop
productName: Lumosnap

# Application icons - electron-builder will use these for all platforms
icon: build/icon

# Deep link protocol registration
protocols:
  - name: Lumosnap
    schemes:
      - lumosnap

directories:
  buildResources: build
files:
  - '!**/.vscode/*'
  - '!src/*'
  - '!electron.vite.config.{js,ts,mjs,cjs}'
  - '!{.eslintcache,eslint.config.mjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,CHANGELOG.md,README.md}'
  - '!{.env,.env.*,.npmrc,pnpm-lock.yaml}'
  - '!{tsconfig.json,tsconfig.node.json,tsconfig.web.json}'
asarUnpack:
  - resources/**
  - '**/node_modules/sharp/**/*'
  - '**/node_modules/@img/**/*'
# Extra resources to include in the app (for tray icons, etc.)
extraResources:
  - from: "build/icon.ico"
    to: "icon.ico"
  - from: "resources/icon.png"
    to: "icon.png"
win:
  executableName: Lumosnap
  icon: build/icon.ico
nsis:
  artifactName: ${name}-${version}-setup.${ext}
  shortcutName: ${productName}
  uninstallDisplayName: ${productName}
  createDesktopShortcut: always
mac:
  icon: build/icon.icns
  entitlementsInherit: build/entitlements.mac.plist
  extendInfo:
    - NSCameraUsageDescription: Application requests access to the device's camera.
    - NSMicrophoneUsageDescription: Application requests access to the device's microphone.
    - NSDocumentsFolderUsageDescription: Application requests access to the user's Documents folder.
    - NSDownloadsFolderUsageDescription: Application requests access to the user's Downloads folder.
    # Deep link URL scheme for macOS
    - CFBundleURLTypes:
        - CFBundleURLSchemes:
            - lumosnap
  notarize: false
dmg:
  artifactName: ${name}-${version}.${ext}
linux:
  icon: build/icons/png
  target:
    - AppImage
    - snap
    - deb
  maintainer: electronjs.org
  category: Utility
  # Deep link MIME type for Linux desktop entries
  mimeTypes:
    - x-scheme-handler/lumosnap
appImage:
  artifactName: ${name}-${version}.${ext}
npmRebuild: true
publish:
  provider: github
  owner: lumosnap
  repo: https://github.com/lumosnap/lumo-desktop
